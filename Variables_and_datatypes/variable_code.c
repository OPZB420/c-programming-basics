#include <stdio.h>
int main()

{
    int num;
    //num =10; // Variable value is pre defined
    // now take user input to define the variable.
    printf("Enter any number: ");
    scanf("%d",&num);
    printf("Num is: %d\n",num);
    return 0;
}
